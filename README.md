# InterviewMate Previous Version

This repository contains the earlier InterviewMate source copied from D:\copy before the live-agent rewrite direction.

## Structure

- backend: FastAPI backend, question bank, resume/GitHub parsing, manual audio answer submission, evaluation, reports.
- frontend: Flutter app with resume/skill setup, manual interview flow, avatar UI, reports, admin screens.

## Important

Real env files, local database, logs, virtual environments, generated storage, Flutter build output, and large local model folders are intentionally not committed.

Copy .env.example to .env and fill local values privately.
